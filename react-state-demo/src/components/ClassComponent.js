import React from 'react'

class CC extends React.Component {
    // constructor(props) {
    //     super(props)
    //     this.state = {
    //         count: 0
    //     }
    // }

    render() {
        return (
            <div>
                Class Component
                {/* <span>current count is: {this.state.count}</span>
                <div>
                    <button onClick={() =>
                        this.setState({ count: this.state.count + 1 })}>
                        increase count
                    </button>
                    <button onClick={() =>
                        this.setState({ count: this.state.count - 1 })}>
                        decrease count
                    </button>
                </div> */}
            </div>)
    }
}

export default CC